using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GMNode : MonoBehaviour
{
    private float x = 10000000.9f;
    // Start is called before the first frame update
    void Start()
    {

        Debug.LogError(x);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
